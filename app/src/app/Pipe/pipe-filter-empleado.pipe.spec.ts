import { PipeFilterEmpleadoPipe } from './pipe-filter-empleado.pipe';

describe('PipeFilterEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
